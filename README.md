# 🏠 Property Listing Platform (Frontend Only)

A fully **frontend-focused real estate property listing platform** built with **React (Vite)**.  
It allows users to browse, search, and filter property listings (for rent/sale) with detailed information and realistic images.  
The design emphasizes **clean UI**, **smooth interaction**, and **responsiveness across all devices**.

---

## 🚀 Project Overview

In today's competitive real estate market, users want an easy way to explore properties, filter them by location, price, or type, and contact agents.  
This project simulates that experience with a **frontend-only prototype** using static JSON data — ideal for demo or UI showcase purposes.

---

## 🧩 Features

✅ View property listings with images, prices, and details  
✅ Filter and search properties by **type**, **location**, or **price**  
✅ Responsive layout for desktop, tablet, and mobile  
✅ Custom CSS (not Tailwind) for styling  
✅ Interactive inquiry button (demo only)  
✅ Modular React component structure  

---

## 🛠️ Technologies Used

| Technology | Purpose |
|-------------|----------|
| **React (Vite)** | Frontend framework for component-based UI |
| **JavaScript (ES6+)** | Logic and interactivity |
| **CSS3** | Custom styling |
| **JSON** | Static data storage for property listings |
| **HTML5** | Page structure and semantics |

---

## 📁 Folder Structure

property-listing-app/
│
├── public/
│ └── favicon.ico
│
├── src/
│ ├── assets/ # Images or static files
│ ├── components/ # Reusable React components (Navbar, PropertyCard, etc.)
│ ├── data/
│ │ └── properties.json # Static property listing data (Bihar-based)
│ ├── pages/ # Main pages (Home, PropertyDetail, About)
│ ├── styles/
│ │ └── style.css # Custom CSS styles
│ ├── App.jsx # Main app entry
│ ├── main.jsx # React entry point
│ └── index.html
│
├── package.json
└── README.md


---

## ⚙️ Installation & Setup

Follow these steps to run the project locally:

1️⃣ Clone the repository:
```bash
git clone https://github.com/yourusername/property-listing-app.git


2️⃣ Go into the project folder:

cd property-listing-app


3️⃣ Install dependencies:

npm install


4️⃣ Run the app:

npm run dev


5️⃣ Open your browser and visit:

http://localhost:5173